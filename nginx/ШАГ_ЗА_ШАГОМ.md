# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ - ЧТО ДЕЛАТЬ И В КАКОМ ПОРЯДКЕ

## 🎯 ОБЩАЯ СХЕМА

```
ШАГ 1: Настройка Nginx и SSL (на сервере)
    ↓
ШАГ 2: Копирование файлов проекта (с вашего компьютера на сервер)
    ↓
ШАГ 3: Настройка Django (на сервере)
    ↓
ШАГ 4: Настройка Next.js (на сервере)
    ↓
ШАГ 5: Запуск сервисов (на сервере)
```

---

## ✅ ШАГ 1: НАСТРОЙКА NGINX И SSL (ПЕРВЫЙ ШАГ!)

**Выполняется на вашем компьютере, но настраивает сервер**

### Вариант А: Автоматически (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Установите sshpass (если еще не установлен)
sudo apt-get install sshpass

# 2. Перейдите в папку nginx
cd "/home/ehson/Рабочий стол/aplrpojjfinal/nginx"

# 3. Запустите скрипт установки
bash run_install.sh
```

**Что делает этот скрипт:**
- ✅ Устанавливает Nginx на сервер
- ✅ Настраивает конфигурацию Nginx
- ✅ Устанавливает SSL сертификаты
- ✅ Настраивает Gunicorn
- ✅ Создает systemd сервис для Django

### Вариант Б: Вручную

Следуйте инструкциям в файле `QUICK_INSTALL.txt`

---

## ✅ ШАГ 2: КОПИРОВАНИЕ ФАЙЛОВ ПРОЕКТА

**Выполняется на вашем компьютере**

После того, как Nginx настроен, скопируйте файлы проекта на сервер:

```bash
# Из корня проекта (aplrpojjfinal)
rsync -avz \
  --exclude 'node_modules' \
  --exclude '__pycache__' \
  --exclude '.next' \
  --exclude 'venv' \
  --exclude '.git' \
  --exclude 'db.sqlite3' \
  "/home/ehson/Рабочий стол/aplrpojjfinal/" \
  root@89.23.100.163:/var/www/aplus/
```

Или используйте скрипт:
```bash
cd "/home/ehson/Рабочий стол/aplrpojjfinal/nginx"
bash copy_project.sh
```

---

## ✅ ШАГ 3: НАСТРОЙКА DJANGO (на сервере)

**Выполняется на сервере - подключитесь через SSH:**

```bash
ssh root@89.23.100.163
```

Затем выполните:

```bash
# 1. Перейдите в папку Django
cd /var/www/aplus/content_api

# 2. Создайте виртуальное окружение
python3 -m venv venv

# 3. Активируйте виртуальное окружение
source venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt
pip install gunicorn

# 5. Настройте settings.py
nano content_api/settings.py
# Измените:
#   DEBUG = False
#   ALLOWED_HOSTS = ['aplus.tj', 'www.aplus.tj', '89.23.100.163']

# 6. Примените миграции
python manage.py migrate

# 7. Соберите статические файлы
python manage.py collectstatic --noinput

# 8. Создайте суперпользователя
python manage.py createsuperuser
```

---

## ✅ ШАГ 4: НАСТРОЙКА NEXT.JS (на сервере)

**Все еще на сервере:**

```bash
# 1. Перейдите в папку Next.js
cd /var/www/aplus/frontenda/Learning-center-A-Client

# 2. Установите зависимости
npm install

# 3. Создайте файл с переменными окружения
echo "NEXT_PUBLIC_API_URL=https://aplus.tj" > .env.local

# 4. Соберите проект
npm run build
```

---

## ✅ ШАГ 5: ЗАПУСК СЕРВИСОВ (на сервере)

**Все еще на сервере:**

```bash
# 1. Запустите Django сервис
systemctl start aplus-django

# 2. Перезапустите Nginx
systemctl restart nginx

# 3. Проверьте статус
systemctl status aplus-django
systemctl status nginx

# 4. Проверьте работу сайта
curl -I https://aplus.tj
```

---

## 📝 КРАТКАЯ СХЕМА ДЛЯ БЫСТРОГО СТАРТА

### На вашем компьютере:

```bash
# 1. Установка Nginx и SSL
cd "/home/ehson/Рабочий стол/aplrpojjfinal/nginx"
sudo apt-get install sshpass
bash run_install.sh

# 2. Копирование файлов
bash copy_project.sh
```

### На сервере (ssh root@89.23.100.163):

```bash
# 3. Django
cd /var/www/aplus/content_api
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt gunicorn
nano content_api/settings.py  # DEBUG=False, ALLOWED_HOSTS
python manage.py migrate
python manage.py collectstatic --noinput
python manage.py createsuperuser

# 4. Next.js
cd /var/www/aplus/frontenda/Learning-center-A-Client
npm install
echo "NEXT_PUBLIC_API_URL=https://aplus.tj" > .env.local
npm run build

# 5. Запуск
systemctl start aplus-django
systemctl restart nginx
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

**Q: Почему сначала Nginx, а не проект?**
A: Потому что Nginx нужно настроить ДО того, как мы скопируем файлы. Конфигурация Nginx должна быть готова, чтобы потом просто перезапустить его.

**Q: Можно ли поменять порядок?**
A: Нет, порядок важен. Если скопировать файлы до настройки Nginx, сервер не будет знать, как их обрабатывать.

**Q: Что если что-то пойдет не так?**
A: Проверьте логи:
- `journalctl -u aplus-django -f` - логи Django
- `tail -f /var/log/nginx/error.log` - логи Nginx

---

## 🎯 ИТОГО: ПОРЯДОК ДЕЙСТВИЙ

1. ✅ **NGINX ПЕРВЫМ** - настройка сервера и SSL
2. ✅ Копирование файлов проекта
3. ✅ Настройка Django
4. ✅ Настройка Next.js
5. ✅ Запуск всех сервисов


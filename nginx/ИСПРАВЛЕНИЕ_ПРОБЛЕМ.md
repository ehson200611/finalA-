# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМ

## Проблемы:
1. ❌ Django сервис не запущен (inactive/dead)
2. ❌ SSL сертификат самоподписанный (браузер не доверяет)
3. ❌ Nginx возвращает 404 (Django не отвечает)

---

## ✅ РЕШЕНИЕ 1: Запуск Django сервиса

**Выполните на сервере:**

```bash
# 1. Проверьте статус сервиса
sudo systemctl status aplus-django

# 2. Запустите сервис
sudo systemctl start aplus-django

# 3. Проверьте логи, если не запускается
sudo journalctl -u aplus-django -n 50

# 4. Проверьте, что Django отвечает
curl http://127.0.0.1:8000/api/
```

**Если сервис не запускается, проверьте:**

```bash
# Проверьте, что gunicorn установлен
cd ~/finalA-/content_api
source venv/bin/activate
which gunicorn

# Проверьте конфигурацию вручную
gunicorn --config nginx/gunicorn_config.py content_api.wsgi:application
```

---

## ✅ РЕШЕНИЕ 2: Обновление путей в Nginx

**Пути в nginx.conf должны указывать на `/root/finalA-`**

Выполните на сервере:

```bash
# 1. Откройте конфигурацию Nginx
sudo nano /etc/nginx/sites-available/aplus

# 2. Замените все пути:
#    /home/ehson/Рабочий\ стол/A+\ pr/
#    на
#    /root/finalA-/

# Или выполните автоматическую замену:
sudo sed -i 's|/home/ehson/Рабочий\\ стол/A+\\ pr|/root/finalA-|g' /etc/nginx/sites-available/aplus

# 3. Проверьте конфигурацию
sudo nginx -t

# 4. Перезапустите Nginx
sudo systemctl restart nginx
```

**Важные пути для замены:**
- `/home/ehson/Рабочий\ стол/A+\ pr/content_api/staticfiles/` → `/root/finalA-/content_api/staticfiles/`
- `/home/ehson/Рабочий\ стол/A+\ pr/content_api/media/` → `/root/finalA-/content_api/media/`
- `/home/ehson/Рабочий\ стол/A+\ pr/frontenda/Learning-center-A-Client/.next` → `/root/finalA-/frontenda/Learning-center-A-Client/.next`

---

## ✅ РЕШЕНИЕ 3: SSL Сертификат

### Вариант А: Установить Let's Encrypt (РЕКОМЕНДУЕТСЯ)

**Это даст вам настоящий, доверенный сертификат:**

```bash
# 1. Установите Certbot
sudo apt update
sudo apt install -y certbot python3-certbot-nginx

# 2. Получите сертификат (автоматически настроит Nginx)
sudo certbot --nginx -d aplus.tj -d www.aplus.tj

# 3. Certbot автоматически:
#    - Получит сертификат
#    - Обновит конфигурацию Nginx
#    - Настроит автоматическое обновление

# 4. Проверьте автопродление
sudo certbot renew --dry-run
```

**Требования для Let's Encrypt:**
- ✅ Домен `aplus.tj` должен указывать на IP сервера (89.23.100.163)
- ✅ Порты 80 и 443 должны быть открыты
- ✅ Nginx должен быть запущен

### Вариант Б: Временно использовать HTTP (для теста)

**Если нужно быстро протестировать без SSL:**

```bash
# 1. Создайте временную конфигурацию без SSL
sudo nano /etc/nginx/sites-available/aplus-http

# 2. Вставьте конфигурацию (см. ниже)

# 3. Отключите HTTPS конфигурацию
sudo rm /etc/nginx/sites-enabled/aplus

# 4. Включите HTTP конфигурацию
sudo ln -s /etc/nginx/sites-available/aplus-http /etc/nginx/sites-enabled/

# 5. Перезапустите Nginx
sudo systemctl restart nginx
```

**Временная HTTP конфигурация:**

```nginx
server {
    listen 80;
    server_name aplus.tj www.aplus.tj;

    # Логи
    access_log /var/log/nginx/aplus_access.log;
    error_log /var/log/nginx/aplus_error.log;

    client_max_body_size 100M;

    # Статические файлы Django
    location /static/ {
        alias /root/finalA-/content_api/staticfiles/;
        expires 30d;
    }

    # Медиа файлы Django
    location /media/ {
        alias /root/finalA-/content_api/media/;
        expires 7d;
    }

    # API к Django
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Админ панель
    location /admin/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Next.js Frontend
    location / {
        root /root/finalA-/frontenda/Learning-center-A-Client/.next;
        try_files $uri $uri.html $uri/ /index.html;
    }
}
```

### Вариант В: Игнорировать предупреждение (не рекомендуется)

**Браузер будет показывать предупреждение, но сайт будет работать:**

- В Chrome: Нажмите "Дополнительно" → "Перейти на сайт aplus.tj (небезопасно)"
- Это только для тестирования! Не используйте в продакшене.

---

## 📋 ПОЛНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ ДЕЙСТВИЙ

**Выполните на сервере по порядку:**

```bash
# 1. Обновите пути в Nginx
sudo sed -i 's|/home/ehson/Рабочий\\ стол/A+\\ pr|/root/finalA-|g' /etc/nginx/sites-available/aplus
sudo nginx -t
sudo systemctl restart nginx

# 2. Запустите Django сервис
sudo systemctl start aplus-django
sudo systemctl status aplus-django

# 3. Проверьте, что Django отвечает
curl http://127.0.0.1:8000/api/

# 4. Установите Let's Encrypt (или используйте HTTP временно)
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d aplus.tj -d www.aplus.tj

# 5. Проверьте сайт
curl -I https://aplus.tj
```

---

## 🔍 ДИАГНОСТИКА

**Если что-то не работает:**

```bash
# Логи Django
sudo journalctl -u aplus-django -f

# Логи Nginx
sudo tail -f /var/log/nginx/aplus_error.log
sudo tail -f /var/log/nginx/aplus_access.log

# Проверка портов
sudo netstat -tlnp | grep :8000  # Django должен слушать
sudo netstat -tlnp | grep :80     # Nginx HTTP
sudo netstat -tlnp | grep :443    # Nginx HTTPS

# Проверка конфигурации
sudo nginx -t
sudo systemctl status nginx
sudo systemctl status aplus-django
```

---

## ✅ ПРОВЕРКА РАБОТЫ

После исправления проверьте:

```bash
# 1. Django отвечает
curl http://127.0.0.1:8000/api/

# 2. Nginx проксирует запросы
curl -I http://aplus.tj/api/

# 3. HTTPS работает (если установлен Let's Encrypt)
curl -I https://aplus.tj

# 4. Frontend доступен
curl -I https://aplus.tj/
```






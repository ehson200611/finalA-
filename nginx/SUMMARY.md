# Резюме: Настройка Nginx для aplus.tj

## ✅ Что было сделано:

### 1. Конфигурация Nginx
- ✅ Настроен `nginx.conf` для домена **aplus.tj** и **www.aplus.tj**
- ✅ Активирован SSL/HTTPS (порт 443)
- ✅ Настроен редирект HTTP → HTTPS
- ✅ Настроено проксирование API запросов к Django (порт 8000)
- ✅ Настроено обслуживание статических файлов Next.js
- ✅ Настроено обслуживание медиа и статических файлов Django
- ✅ Добавлены настройки безопасности и кеширования

### 2. SSL сертификаты
- ✅ Сохранены сертификаты в текстовом формате:
  - `ssl/aplus.tj.key.txt` - приватный ключ
  - `ssl/aplus.tj.crt.txt` - сертификат
- ✅ Создан скрипт автоматической установки: `install_ssl.sh`
- ✅ Созданы инструкции по установке SSL

### 3. Документация
- ✅ `DEPLOYMENT.md` - подробная инструкция по развертыванию
- ✅ `QUICK_START.md` - быстрый старт
- ✅ `SSL_SETUP.md` - инструкция по установке SSL
- ✅ `SSL_INSTALL.txt` - краткая инструкция по SSL
- ✅ `README.md` - обзор всех файлов

### 4. Дополнительные файлы
- ✅ `gunicorn_config.py` - конфигурация Gunicorn
- ✅ `aplus-django.service` - systemd service файл
- ✅ `nginx-nextjs-server.conf` - альтернативная конфигурация
- ✅ `settings_production.py.example` - пример production настроек

## 📋 Что нужно сделать на сервере:

### Шаг 1: Обновить пути
В файле `nginx.conf` замените пути:
```
/home/ehson/Рабочий\ стол/A+\ pr/
```
на путь на вашем сервере, например:
```
/var/www/aplus/
```

### Шаг 2: Установить SSL сертификаты
```bash
cd nginx
bash install_ssl.sh
```

Или вручную:
```bash
sudo mkdir -p /etc/nginx/ssl
sudo cp ssl/aplus.tj.key.txt /etc/nginx/ssl/aplus.tj.key
sudo cp ssl/aplus.tj.crt.txt /etc/nginx/ssl/aplus.tj.crt
sudo chmod 600 /etc/nginx/ssl/aplus.tj.key
sudo chmod 644 /etc/nginx/ssl/aplus.tj.crt
```

### Шаг 3: Установить конфигурацию Nginx
```bash
sudo cp nginx/nginx.conf /etc/nginx/sites-available/aplus
sudo ln -s /etc/nginx/sites-available/aplus /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### Шаг 4: Настроить Django
- Обновите `ALLOWED_HOSTS = ['aplus.tj', 'www.aplus.tj']`
- Установите `DEBUG = False`
- Настройте базу данных

### Шаг 5: Настроить Next.js
- Создайте `.env.local` с `NEXT_PUBLIC_API_URL=https://aplus.tj`
- Выполните `npm run build`

## 🔒 Безопасность

- ✅ SSL сертификат настроен и активен
- ✅ HTTP автоматически перенаправляется на HTTPS
- ✅ Приватный ключ защищен (chmod 600)
- ✅ Настроены заголовки безопасности

## 📝 Важные замечания

1. **Срок действия сертификата**: до 2026-10-21
2. **Домены**: aplus.tj и www.aplus.tj
3. **Пути**: Обязательно обновите все пути в конфигурации на пути вашего сервера
4. **База данных**: Рекомендуется использовать PostgreSQL для production

## 📚 Документация

- Подробная инструкция: `DEPLOYMENT.md`
- Быстрый старт: `QUICK_START.md`
- Установка SSL: `SSL_INSTALL.txt` или `SSL_SETUP.md`

## ✨ Готово к развертыванию!

Все необходимые файлы созданы и настроены. Следуйте инструкциям в `QUICK_START.md` для быстрого развертывания.

